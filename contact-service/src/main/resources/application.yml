server:
  port: 8081

spring:
  application:
    name: contact-service

  datasource:
    url: jdbc:mysql://localhost:3306/contact_db
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092
