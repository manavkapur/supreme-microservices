spring:
  application:
    name: email-service

  mail:
    host: smtp.zoho.in
    port: 587
    username: info@supremebuildsolutions.com
    password: J8ysjYByKxki
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        ssl:
          trust: smtp.zoho.in


  redis:
    host: localhost
    port: 6379

server:
  port: 8089

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: false
    instance-id: ${spring.application.name}:${server.port}
